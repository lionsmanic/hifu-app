services:
  - type: web
    name: hifu-questionnaire
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: EMAIL_USER
        value: cgmh.hifu.staff@gmail.com   # 寄信帳號
      - key: EMAIL_PASSWORD
        sync: false          # ← Render Dashboard 手動填入 App Password（不要寫在這裡）
      - key: EMAIL_RECEIVER
        value: cgmh.hifu.center@gmail.com  # 收信帳號
